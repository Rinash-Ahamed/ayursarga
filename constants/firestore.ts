export const COLLECTIONS = {
  users: "users",
  hospitals: "hospitals",
  services: "services",
  bookings: "bookings",
} as const;

export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 50;
